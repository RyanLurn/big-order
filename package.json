{
  "name": "big-order",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "format": "prettier . --write --cache",
    "format:check": "prettier . --check --cache",
    "brand:dev": "pnpm --filter brand run dev",
    "brand:type-gen": "pnpm --filter brand run type-gen",
    "brand:type-check": "pnpm --filter brand run type-check"
  },
  "devDependencies": {
    "@prettier/plugin-oxc": "0.0.5",
    "prettier": "3.6.2",
    "prettier-plugin-curly": "0.4.0"
  },
  "packageManager": "pnpm@10.23.0"
}
